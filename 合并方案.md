# 两个简历项目合并方案

## 合并目标

把桌面 AI产品经理 目录下两个简历相关项目合并为一个主项目：

主项目：
`/Users/mahaoxuan/Desktop/AI产品经理/项目D-简历PDF生成器`

被合并旧项目：
`/Users/mahaoxuan/Desktop/AI产品经理/resume-pdf-builder`

## 判断

`项目D-简历PDF生成器` 应作为主项目，因为它已经包含：

1. 可信投递系统 Demo 网页：`web/可信投递系统Demo.py`
2. Route B 可信投递实验输出。
3. 最新正式投递带照片候选版 v2。
4. 事实索引、JD匹配矩阵、事实核查、视觉核查、独立检查Agent摘要。
5. 已验证的照片提取、PDF生成、预览图、测试文件。

`resume-pdf-builder` 保留价值主要是：

1. 早期 Markdown → HTML/PDF 生成器。
2. 旧版 HTML 模板：default / modern / ats-friendly / classic。
3. 早期 Web main.py 与 CLI 生成脚本。
4. 示例输出和中文Demo版本。

## 合并方式

不直接覆盖主项目现有核心代码。

将旧项目整体移入主项目：

`项目D-简历PDF生成器/legacy/resume-pdf-builder/`

这样做的好处：

1. 顶层只保留一个主项目。
2. 旧项目完整可追溯，不丢文件。
3. 主项目当前网页 Demo 和最新输出不受影响。
4. 后续如果要复用旧 HTML 模板，可从 legacy 中逐步抽取到正式模板系统。

## 合并后主项目结构建议

```text
项目D-简历PDF生成器/
  core/                         # 当前核心代码
  scripts/                      # 当前生成脚本，包括可信投递生成脚本
  web/                          # 当前Web入口，包括可信投递系统Demo
  tests/                        # 当前验证测试
  templates/                    # 可继续承载HTML模板
  output/                       # 所有验证输出
  legacy/resume-pdf-builder/    # 旧项目完整归档
  README.md                     # 更新为可信投递系统主README
  合并方案.md
  合并完成说明.md
```

## 后续产品路线

1. 网页 Demo 使用 `项目D-简历PDF生成器` 作为唯一根目录。
2. 短期继续复用已生成的 PDF/HTML/预览图做可点击验证。
3. 中期把旧 `templates/` 中的 HTML 风格模板抽象成“模板库”。
4. 长期将核心从 Markdown/HTML 迁移为：结构化简历数据 → 模板选择 → PDF渲染 → 技术/事实/视觉核查。
