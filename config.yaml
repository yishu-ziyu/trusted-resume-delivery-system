llm:
  provider: ${LLM_PROVIDER}
  api_key: ${LLM_API_KEY}
  base_url: ${LLM_BASE_URL}
  model: ${LLM_MODEL}

paths:
  templates_dir: "./templates"
  output_dir: "./output"

pdf:
  default_template: "default.html"
  page_size: "A4"
