# 项目D：BDD + TDD 快速验证工作流

> 这份文档把用户知识库里的 BDD/TDD 软约束，落到“简历 PDF 生成器”这个项目里。

参考笔记：

- `/Users/mahaoxuan/Desktop/知识库/知识库/02 Wiki 维基/AI与工具/BDD_TDD_AI编程双保险.md`
- `/Users/mahaoxuan/Desktop/知识库/知识库/02 Wiki 维基/个人体系/AI_Coder_BDD_TDD软约束工作流.md`
- `/Users/mahaoxuan/Desktop/知识库/知识库/01 Raw 原始数据/text/2026-05-08/TDD约束AI写代码_用户粘贴转写.md`
- `/Users/mahaoxuan/Desktop/知识库/知识库/01 Raw 原始数据/text/2026-05-08/BDD_TDD约束AI写代码_用户粘贴转写.md`
- `/Users/mahaoxuan/Desktop/知识库/知识库/02 Wiki 维基/Sources/2026-05-08_用户粘贴_TDD约束AI写代码.md`
- `/Users/mahaoxuan/Desktop/知识库/知识库/02 Wiki 维基/Sources/2026-05-08_用户粘贴_BDD_TDD约束AI写代码.md`

---

## 1. 合作机制

这个项目的合作方式不是先写大而全的规划，再长期闭门开发。

默认机制是：

```text
目标
→ 最快验证
→ 看结果是否符合需求
→ 如果方向对，再继续加要求
→ 如果方向不对，及时换路线
```

也就是说，每次新想法先做一个可观察的实验。

例如这次“PDF 简历模板是否可复用”，不是先写完整产品，而是先做：

- 原 PDF 文本层数字替换测试。
- 生成一版 AI 产品方向展示简历。
- 观察版式、图片、ATS 友好性和人读效果。

这个方式适合当前项目，因为 PDF 简历生成的难点很多：可编辑性、排版、照片、ATS、用户确认、事实边界。很多判断不能靠想象，只能靠小实验验证。

---

## 2. BDD 和 TDD 在这个项目里的分工

知识库里的核心判断是：

- BDD 负责确认“是不是我要的行为”。
- TDD 负责确认“代码有没有稳定实现这个行为”。

放到本项目里就是：

| 层级 | 在本项目中的作用 | 用户主要审查什么 | Agent 主要做什么 |
|---|---|---|---|
| BDD | 锁定产品行为 | Given / When / Then 是否符合真实需求 | 把模糊需求拆成可验收场景 |
| TDD | 锁定模块行为 | 测试是否覆盖已确认行为 | 先写失败测试，再写最小实现 |
| 视觉验收 | 判断简历是否真的好看、克制、高级 | PDF 预览、截图、可读性 | 生成预览图并说明风险 |
| 事实审查 | 防止简历内容过度包装或编造 | 哪些表述能否被面试追问 | 标出新增表述和事实来源 |
| 回归验证 | 防止改一个能力坏另一个能力 | 旧样例是否还可用 | 跑测试、生成样例、汇报差异 |

---

## 3. 默认执行协议

之后每做一个项目能力，默认按下面顺序。

### Step 1：先写 BDD，不写实现

先把需求变成 3-8 条 Given / When / Then。

例子：

```gherkin
Scenario: 文本型 PDF 可以提取照片并用于展示版简历
  Given 用户上传了一份包含证件照的文本型 PDF 简历
  When 系统执行 PDF 资源提取
  Then 系统应识别出最可能的头像图片
  And 系统应保存该图片到 output/assets
  And 展示版简历可以引用该图片
```

此时不写实现。先确认行为是否对。

### Step 2：BDD 转 TDD 测试

把已确认行为转成测试。

测试要做到：

- 测试名直接说明业务行为。
- 输入对应 Given。
- 操作对应 When。
- 断言对应 Then。
- 先运行，确认失败。

### Step 3：写最小实现

只写让测试通过所需的最小代码。

不做无关重构。
不削弱测试。
不为了通过测试改变需求。

### Step 4：真实验收

测试通过后，还要看真实 PDF / 预览图。

因为简历项目有很多视觉和体验问题，单元测试不能完全覆盖：

- 是否好看。
- 是否高级但克制。
- 是否可读。
- 是否内容过密。
- 是否适合 ATS。
- 是否适合人读。
- 照片是否自然。

### Step 5：汇报剩余风险

每次交付都要说明：

- 实现了哪些行为。
- 哪些测试覆盖了这些行为。
- 运行过哪些命令。
- 输出文件在哪里。
- 哪些地方还需要用户人工判断。

---

## 4. 本项目的两类版本

当前判断：系统应该默认产出两类简历。

### 4.1 ATS 稳定版

目标：适合网申、ATS 解析、HR 初筛。

行为要求：

```gherkin
Scenario: 生成 ATS 友好版简历
  Given 用户已有结构化简历内容
  And 用户选择 ATS 稳定版
  When 系统生成 PDF
  Then PDF 应为白底或极浅色背景
  And 核心信息应全部是可复制文本
  And 标题应使用标准名称，如教育经历、实习经历、项目经历、技能
  And 不应依赖图片、图标或复杂卡片传递核心信息
```

设计原则：

- 结构标准。
- 视觉克制。
- 少用 sidebar。
- 少用卡片。
- 不把文字放进图片。
- 保证关键词可复制、可搜索。

### 4.2 人读展示版

目标：适合内推、面试官、个人网站、邮件发送。

行为要求：

```gherkin
Scenario: 生成带照片的人读展示版简历
  Given 用户原 PDF 中包含证件照
  And 用户选择人读展示版
  When 系统生成 PDF
  Then PDF 应保留或复用用户照片
  And 视觉设计应克制、有层级、不过度炫技
  And 核心经历和技能仍应可复制
  And 不应出现文字重叠、裁切或信息过密
```

设计原则：

- 可以有照片。
- 可以有侧边栏。
- 可以有卡片。
- 可以体现产品感和个人气质。
- 但所有视觉元素都必须服务信息表达。

---

## 5. 当前已经验证过的事实

### 5.1 原 PDF 不是纯图片扫描版

`/Users/mahaoxuan/Desktop/马浩宣简历.pdf` 可以提取文本 span。

已经做过数字替换测试：

- 输出：`output/clone_test/mahaoxuan_resume_digits_replaced.pdf`
- 原姓名不再出现在抽取文本中。
- 抽取文本只剩数字和换行。
- 报告显示替换了 78 个 text spans。

结论：这份 PDF 至少有可编辑/可覆盖的文本层。

### 5.2 原 PDF 中可以提取照片

已经从原 PDF 中提取出疑似证件照：

`output/assets/original_p1_img5.jpeg`

这说明后续展示版可以复用原照片。

### 5.3 已生成一版人读展示草稿

输出：

- `output/mahaoxuan_ai_pm_resume.pdf`
- `output/mahaoxuan_ai_pm_resume.html`
- `output/mahaoxuan_ai_pm_resume.md`
- `output/mahaoxuan_ai_pm_resume_preview.png`

这版更偏“人读展示版”，不是最稳的 ATS 版。

---

## 6. 下一轮最小验证目标

下一轮建议只验证一个小目标：

> 能否基于同一份增强内容，同时生成 ATS 稳定版和带照片的人读展示版？

不要一开始做完整系统。

### BDD 草案

```gherkin
Feature: 双版本简历生成

Scenario: 生成 ATS 稳定版
  Given 已有增强版 Markdown 简历内容
  When 系统生成 ATS 版本 PDF
  Then 输出 PDF 应为一页或两页
  And 所有核心文本应可复制
  And 不应依赖照片、图标、深色背景或复杂卡片传递核心信息

Scenario: 生成带照片的人读展示版
  Given 已有增强版 Markdown 简历内容
  And 原 PDF 中已提取出头像图片
  When 系统生成展示版 PDF
  Then 输出 PDF 应包含头像
  And 视觉风格应克制、统一、有层级
  And 不应出现文字重叠、裁切或不可读区域

Scenario: 两个版本内容一致但排版目标不同
  Given ATS 版和展示版都已生成
  When 用户比较两版文本内容
  Then 两版应保留同一组核心经历、教育、项目和联系方式
  And ATS 版更偏结构标准
  And 展示版更偏视觉呈现
```

### TDD 草案

下一步可以先写测试：

- `tests/test_photo_extractor.py`
  - 确认能从原 PDF 提取头像候选。

- `tests/test_resume_versions.py`
  - 确认 ATS 版和展示版都能生成。
  - 确认 PDF 页数大于 0。
  - 确认 PDF 文本包含姓名、邮箱、AI Agent 等核心文本。

- `tests/test_render_quality.py`
  - 确认输出 PDF 无空文件。
  - 确认生成预览图。
  - 后续再加文本重叠和溢出检测。

---

## 7. 最终交付格式

每一轮实验完成后，按这个格式汇报。

```text
## 行为覆盖
- [x] 行为 1：...
- [x] 行为 2：...
- [ ] 未覆盖行为：...

## 测试覆盖
- 测试文件：...
- 运行命令：...
- 结果：...

## 输出文件
- PDF：...
- HTML：...
- Markdown：...
- Preview：...

## 手动验收
1. 打开 PDF 看整体版式。
2. 复制 PDF 文本，看关键信息是否存在。
3. 检查照片是否自然。
4. 检查是否适合 ATS 或人读场景。

## 剩余风险
- ...
```

---

## 8. 项目提示词

以后在这个项目里，可以直接使用这段约束：

```text
在项目D-简历PDF生成器中，请默认采用“最快验证 + BDD + TDD”的协作方式。

先不要直接做完整系统。每次先明确一个最小验证目标。

流程：
1. 先把目标写成 3-8 条 Given / When / Then 行为用例。
2. 我确认后，再把行为用例转成自动化测试。
3. 测试必须先失败，确认它真的约束了新行为。
4. 再写最小实现让测试通过。
5. 生成真实 PDF / 预览图做人工验收。
6. 最后汇报行为覆盖、测试覆盖、输出文件和剩余风险。

视觉设计必须克制且高级。不要为了炫技而炫技。
ATS 版优先稳定解析，人读展示版可以更有设计感并保留照片。
```
